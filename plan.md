firebase firestore

nedb for local storage

https://www.npmjs.com/package/rpi-gpio

usercollection schema
    id int unique
    name
    hashedCode
    startDate
    endDate

keyed code id + . + code
